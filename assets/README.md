# assets

Raw imported files that are not scenes or scripts.

- `sprites/` — sprite sheets and individual textures for all entities, UI, and environment tiles
- `audio/sfx/` — one-shot sound effects (hits, pickups, UI clicks)
- `audio/music/` — looping background tracks per level or menu state
- `fonts/` — .ttf / .otf font files used by UI themes
