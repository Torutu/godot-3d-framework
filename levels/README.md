# levels

One folder (or .tscn file) per level in the linear progression.

Each level scene contains the tilemap, enemy spawn points, loot spawn points, and any
level-specific triggers or scripted events. Name levels numerically (level_01, level_02 …)
so load order is obvious.
