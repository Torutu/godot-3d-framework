# core

Game-wide systems and scenes that exist outside of individual levels.

- `autoloads/` — singleton scripts registered in Project Settings (e.g. GameState, AudioManager, SaveManager)
- `main_menu/` — main menu scene and its logic (new game, continue, quit)
- `game_over/` — game-over / win screen scene and transition logic
