# entities

Self-contained scenes for anything that lives in the game world.

- `player/` — player scene, controller script, stat resource, and animations
- `enemies/` — one sub-folder per enemy type; each contains its scene, AI script, and stat resource
- `loot/` — loot item scenes, drop-table resource, and pickup logic
